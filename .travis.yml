language: java
sudo: false

cache:
  directories:
  - $HOME/.m2
  
matrix:
  include:
    - env: PROFILES='-P osgi-tests'
      jdk: openjdk17
    - env: PROFILES='-P osgi-tests'
      jdk: openjdk18

script:
  - echo JAVA_HOME = ${JAVA_HOME}
  - mvn clean install -B $PROFILES